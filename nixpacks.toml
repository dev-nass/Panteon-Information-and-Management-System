[phases.setup]
nixPkgs = ["php83", "php83Extensions.zip", "php83Extensions.gd", "composer", "nodejs"]

[phases.build]
cmds = [
  "composer install --optimize-autoloader --no-dev --no-interaction",
  "npm install",
  "npm run build",
]
